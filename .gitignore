# Coq build artifacts
rocq/*.vo
rocq/*.vos
rocq/*.vok
rocq/*.glob
rocq/.*.aux
rocq/.lia.cache
rocq/.nia.cache
rocq/Makefile
rocq/Makefile.conf
rocq/.Makefile.d
rocq/audit_kernel.ml
rocq/audit_kernel.mli

# The extracted kernel is regenerated by `make ocaml` and copied here;
# it is not hand-edited, so it is not tracked either.
ocaml/audit_kernel.ml
ocaml/audit_kernel.mli

# dune
_build/
*.install
.merlin

# editor / OS
.DS_Store
*.swp

# LaTeX build artifacts (docs/*.pdf is tracked; these are not)
docs/*.aux
docs/*.log
docs/*.out
docs/*.bbl
docs/*.blg
docs/*.toc
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz
